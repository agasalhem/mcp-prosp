# API key do Prosp.ai (obrigatório)
PROSP_API_KEY=sua_chave_aqui

# Transporte: 'stdio' (default) ou 'http'
MCP_TRANSPORT=stdio

# Porta HTTP (default: 3000, só usado quando MCP_TRANSPORT=http)
PROSP_MCP_PORT=3000

# Token Bearer para proteger o endpoint HTTP (opcional)
MCP_AUTH_TOKEN=
